# خريطة الأدوار — منصة حلقات البراك

> **مرجعٌ ملزِم.** يُقرأ قبل أيّ عملٍ يمسّ الصلاحيّات. أقرّه محمد بالحوار.
> الوكيل يبني منه لا من ذاكرته؛ وأيّ حكمٍ غير مذكورٍ هنا ⟵ يتوقّف ويسأل.
>
> صلاحيّات **التسكين وتغيير البرنامج والمسار**: انظر [PLACEMENT_RULES.md](PLACEMENT_RULES.md) › ق٧.

## مبدأ الفصل

النظام يفصل ثلاث طبقات سلطةٍ لا تُخلط: **تقنيّة مطلقة / حوكمة وإشراف / تشغيل تربويّ**.

الأدوار تُخزَّن مصفوفةً (`User.roles`)، فيجوز أن يحمل شخصٌ واحدٌ أكثر من دورٍ معاً.

---

## الطبقات الثلاث العليا

### المدير التقنيّ (TECH_ADMIN) — جديد، القمّة المطلقة

- يملك **كلّ الصلاحيّات بلا استثناء**: كلّ التشغيل التربويّ + إدارة المستخدمين + الرؤية الكاملة + أيّ صلاحيّةٍ تقنيّة.
- يدير المستخدمين كاملاً: إنشاء، تعطيل، تصعيد/خفض الأدوار — **بما فيها تعيين مشرفٍ عامّ، أو مدير حلقات، أو مديرٍ تقنيٍّ آخر**.
- هو مفتاح المنصّة كلّها. (اليوم: محمد.)

### المشرف العام (SUPER_ADMIN) — إشرافيّ/حوكميّ

- **يرى كلّ شيء** بلا استثناء (كلّ الحلقات، الطلاب، التقارير، الأرصدة) — اطّلاعٌ كامل.
- يملك **إدارة المستخدمين** (إضافة، تعطيل، تصعيد، منح الأدوار).
- **يُمنع من قرارات التشغيل التربويّ**: لا منح/خصم نقاط، لا اعتماد انتقال/تخرّج، لا تعيين معلّمين/عرفاء، لا قبول طلبات قيد.
- المقصود أن يكون شخصاً واحداً إشرافياً (لا يلزم النظام بذلك تقنيّاً، لكنّه العُرف).

### مدير الحلقات (CIRCLE_MANAGER) — التشغيل التربويّ

- يملك **كلّ قرارات التشغيل التربويّ**: النقاط، الاعتمادات (انتقال/تخرّج)، التعيينات (معلّمين/عرفاء)، قبول القيد، إدارة الحلقات، اختبار المقاطع…
- **لا يملك إدارة المستخدمين ولا منح الأدوار** (هذه للمشرف والمدير التقنيّ فقط).

---

## ملاحظات

- شخصٌ واحدٌ قد يجمع أكثر من دور (مثل محمد: مدير تقنيّ + مدير حلقات معاً).
- الحارس يفهم «الأدوار المجتمعة»: من يحمل عدّة أدوارٍ يملك اتّحاد صلاحيّاتها.
- قاعدة الحماية: لا يخفِض أحدٌ دوره الأعلى عن نفسه بما يقفل المنصّة؛ ويُمنع منطقياً بقاء النظام بلا مدير تقنيٍّ فعّال.
- **الحالة الراهنة (دَينٌ تقنيّ للتنفيذ لاحقاً):** الأدوار الحاليّة في enum هي `SUPER_ADMIN`, `CIRCLE_MANAGER`, `REGISTRAR`, `TEACHER`, `RECITER`, `ARIF`, `STUDENT`, `GUARDIAN`. الدور `TECH_ADMIN` جديدٌ سيُضاف. ومعنى `SUPER_ADMIN` اليوم في الحُرّاس = «القمّة المطلقة» (خطأ يجب تصحيحه لاحقاً ليصير إشرافياً فقط). إعادة ترتيب الحُرّاس في الشاشات دفعةٌ مخصّصةٌ منفصلة.

# خطة البناء — منصة حلقات البراك

**مبنيّة على:** `DESIGN.md` (٥٠ قراراً) + `DIAGNOSIS.md` (تقرير المدقّق المستقل)
**قرار المكدَّس:** Next.js + TypeScript + Prisma + Supabase + Vercel — **بمخطط نظيف**
**الظرف الحاسم:** **لا بيانات حقيقية** ⟵ لا هجرة، ولا تحويل، ولا تنازل.

---

## ٠. ما تغيّر بعد التشخيص

| ما كنّا نظنّ | الواقع |
|---|---|
| Next.js + Prisma + TS قائم | **تطبيق ثابت بـJS خام** — `app.js` = ١٤٠٨ أسطر، بلا بناء ولا أنواع |
| «جاهزة كشكل» | **٥٥٪ — لكن الـ٥٥٪ هي الأساس والناس والمصادقة** |
| القلب ينتظر البناء | **صحيح تماماً**: الحصاد ٣٠٪ (ذاكرة)، الموافقات ١٥٪، والمراحل والمسارات والترسيخ **صفر** |

**فقولك في أول رسالة كان دقيقاً — والتقرير يصدّقه.**

### ما يُؤخذ من المنصة القديمة

| ✅ يُؤخذ | ❌ يُترك |
|---|---|
| **قرارات** المخطط: الأنواع (`student_status`, `mistake_type`…) | ٨ ملفات SQL متداخلة — `app_is_staff` معرَّفة **مرتين** |
| **بنية** سياسات RLS بالأدوار (مدروسة) | **`p_all using(true)`** — تسقط بالبنية لا بالانضباط |
| **حيلة المصادقة**: الجوال ← بريد اصطناعي `u<digits>@albrrak.app` | الترحيل اليدوي بالترتيب |
| **`points.source_event`** — بذرة الاقتصاد | الشاشات الوهمية (٨ شاشات 🔴) |
| **`css/styles.css`** (٥٨٠ سطر RTL — أثنى عليه المدقّق) | `admin_link_user` المفقودة |
| **`vercel.json`** (CSP + HSTS — «جيد جداً») | |

> **أثمن ما في منصتك ليس الكود — بل القرارات التي فيه. وهي تنتقل كلها.**

---

## ١. قواعد العمل — تُلزم كل مرحلة

**١ — لا تُلمَس مرحلة مُغلقة.** المرحلة تُغلق باجتياز معيار قبولها. ما بعدها يُضيف ولا يُعدّل.

**٢ — لا commit إلا بعد:**
```bash
npx tsc --noEmit && npm run lint && npm run build
```
**الثلاثة خضراء أو لا commit.** ⟵ **وهذه ليست انضباطاً للمبرمج — بل عينا كلود كود.** الوكيل لا يرى شاشتك؛ يرى مخرجات الأدوات. بلا أنواع وبناء، **يعدّل ويقول «تمّ» ويكسر شاشةً أخرى صامتاً**.

**٣ — Prisma مصدر الحقيقة الوحيد.** لا SQL يدوي في لوحة Supabase أبداً. كل تغيير = ترحيل مرقّم قابل للتراجع.

**٤ — القواعد المطلقة في الخادم.** كل قاعدة من م١–م٦ لها **اختبار** يثبت أن تجاوزها من العميل **يُرفض**.

**٥ — كل تغيير حالة يُصدِر حدثاً** (`Event`). ⟵ تأمينٌ للاقتصاد لاحقاً بلا هجرة.

**٦ — لا شاشة وهمية.** إمّا موصولة بالقاعدة، أو **غير موجودة**. الشاشة التي تُوهم الاكتمال أخطر من غيابها — وقد وقعت مرة.

---

## ٢. المسارات المتوازية — تبدأ فوراً

### 🔴 المهمة (أ) — spike الشهادة العربية

**تسبق المرحلة ٤، وكلما بكّرت كان أسلم.**

**المطلوب:** شهادة واحدة كاملة PNG، بالعربية، باسمٍ حقيقي، وQR — **تُرى بالعين**.

**العلّة:** المكتبات الشائعة **تكسر تشكيل الحروف العربية ووصلَها** ⟵ «ا ل ف ا ت ح ة» مقطّعة أو معكوسة. وهذه ليست شهادةً تُنشر باسم حلقاتٍ في المدينة.

**معيار القبول:** الاسم العربي **موصولٌ مشكولٌ صحيح**، على Vercel لا على الجهاز المحلي.
**إن كسرت:** نغيّر الأداة **قبل** أن يُبنى عليها شيء.

### 🔴 المهمة (ب) — جدول حدود الأحزاب الستين

**تسبق المرحلة ٤. وهي مهمة محمد لا مهمة المبرمج.**

**لن يُستخرج من ذاكرة نموذج لغوي.** خطأ آية واحدة = **طالبٌ يُحصد على غير ما حفظ، وشهادةٌ تصدر على غير أساس**.

**المخرج:** ٦٠ سطراً: `(رقم الحزب، من سورة:آية، إلى سورة:آية)` من **مصحف المدينة**.
**معيار القبول:** **توقيع محمد.**

### 🟠 المهمة (ج) — نماذج الجهة الإشرافية

**ملف الإكسل لا صورته.** العمودان المخفيان • قوائم الاختيار (الجنسية، المرحلة الدراسية) • **صيغة «مقدار حفظ الطالب»**.

> **التقارير تُستدرَك في يوم. الحقول لا تُستدرَك أبداً** — تسأل ٤٥ أسرة عن ماضٍ مضى، أو تفقده.

### 🟠 المهمة (د) — سؤالٌ للجهة

**أتشترط استضافة البيانات داخل المملكة؟**
Supabase وVercel **خارجها**. ⟵ إن ألزمت، فليست المسألة تعديلاً في الكود — **بل تغيير البنية التحتية كلها**. **يُسأل الآن، لا بعد سنة.**

---

## ٣. المراحل

```
م٠ الأساس والأمان
      │
      ▼
م١ الناس والمحرّكات ────────┐
      │                      │
      ▼                      │
م٢ الحضور والقياس            │
      │                      │
      ▼                      ▼
م٣ القاعدة المدنية    (أ) spike الشهادة
      │                      │
      ▼                      │
م٤ مراقي ◄──── (ب) حدود الأحزاب
      │
      ▼
م٥ الشهادات ◄──────────────┘
      │
      ▼
م٦ الاقتصاد (بانتظار الشرح)
      │
      ▼
م٧ البرنامج الأسبوعي (مؤجَّل بقرار)
```

---

### م٠ — الأساس والأمان

**الهدف:** مشروع يبني، ينشر، وقاعدة **مغلقة**.

**المخرجات:**
- مشروع Next.js (App Router) + TypeScript **strict** + Prisma + Tailwind
- `prisma/schema.prisma` — الأنواع والجداول الأساسية فقط
- **ترحيل واحد نظيف** — و`p_all using(true)` **لا وجود لها أصلاً**
- سياسات RLS من Prisma migrations
- CI: `tsc + lint + build` على كل push
- نشر على Vercel
- نقل `styles.css` و`vercel.json`

**معيار القبول:**
```bash
npx tsc --noEmit        # نظيف
npm run lint            # نظيف
npm run build           # نظيف
npx prisma migrate status  # up to date

# الاختبار الحاسم — يجب أن يرجع [] أو 401/403:
curl -H "apikey: <anon>" "<url>/rest/v1/students?select=id&limit=1"
```

> **ملاحظة:** المفتاح العام في git **ليس ثغرة بذاته** — هو مصمَّم ليكون عاماً. **الثغرة هي RLS المفتوحة.** فلا تُدوِّر المفتاح؛ **أغلق السياسة.**

---

### م١ — الناس والمحرّكات

**الهدف:** طالبٌ يُسجَّل، يُقبل، يُنشأ حسابه، ويُربط بوليّه — بصلاحياتٍ محكمة.

**المخرجات:**
- `User` بالأدوار الثمانية (صفات لا كيانات) • `Circle` • `CircleTeacher` (**متعدد لمتعدد**) • `Student` • `Enrollment` (بسجلٍّ تاريخي) • `GuardianLink`
- **نموذج قيد عام بلا دخول** ⟵ القبول يُنشئ الحساب
- القبول • الرفض بسبب • **قائمة الانتظار**
- **سياسة الأعمار**: دون ١٣ لا حساب • ١٣–١٧ ولاية • ١٨+ بإذنه • طلب فكّ الربط (لا يظهر للولي)
- **رقم الهوية**: تشفير + حجب + **سجل اطّلاع**
- **محرّك الاعتمادات** (`Approval`) — واحدٌ لثمانية استعمالات
- **محرّك التفويض** (`PermissionDelegation`) — على مستوى الأدوار
- `Event` — كل تغيير حالة يُصدِر حدثاً

**معيار القبول:**
- طفل عمره ١٢ ← **لا يُنشأ له حساب** (اختبار)
- طالب ١٥ ← حساب + **ولي مرتبط بحكم الولاية** + يُخبَر
- طلب فكّ ربط ← **لا يظهر للولي** في أي استعلام (اختبار)
- **رقم هوية:** معلم يستعلم عنه ← **يُرفض**؛ ومن يراه ← **سطرٌ في سجل الاطّلاع** (اختبار)
- المدير يفوّض «قبول العذر» للمعلم من الشاشة ← **ينفذ بلا نشر**

---

### م٢ — الحضور والقياس

**الهدف:** رصدٌ **يعمل فعلاً** — وقياسٌ لا يكذب.

**المخرجات:**
- `Attendance` — **الحالات الست**
- **الشاشة تفترض الجميع حاضرين، والمعلم يؤشّر الغائب فقط** ⟵ ١٥ طالباً وغاب اثنان = **نقرتان**
- **تعمل بلا إنترنت وتُزامن** ⟵ التغطية داخل المساجد ضعيفة
- «غائب بعذر» و«مستأذن مسبقاً» = **إجراءان بدورة حياة**، لا حالتان
- **مواقيت الصلاة للمدينة** — تُحسب لا تُكتب (الجلسة = ما بين أذانين)
- عدّاد `attendanceDays`
- **مقارنة بوسيط الأقران — بلا حكم**

**معيار القبول:**
- رصد حلقة ١٥ طالباً ← **≤ ٥ ثوانٍ**
- قطع الشبكة أثناء الرصد ← **يُحفظ ويُزامن** (اختبار)
- كل قبول عذر ← **مسجَّلٌ بصاحبه وتفويضه**
- شاشة المدير: *«خالد في الباب الثاني منذ ١٨ **يوم حضور**. وسيط أقرانه: ٩»*
- **لا عتبات مخترعة** — التنبيه معطَّل حتى ٢٠ إتماماً

> **المعادلة:** كل ثانية على المعلم × طلابه × أيام السنة. **فإن ثقُل، رصده من ذاكرته آخر الأسبوع — فصارت البيانات كاذبة.** وأهون من بيانات كاذبة ألّا يكون رصدٌ أصلاً.

---

### م٣ — القاعدة المدنية

**الهدف:** أول برنامج كامل — **من الباب الأول إلى الشهادة**.
**ولماذا قبل مراقي:** أبسط (بلا ترسيخ ولا مراجعة ولا مسارات)، **فيُثبت المحرّكات على مثالٍ يحتمل الخطأ**.

**المخرجات:**
- بذر **١٣ باباً** بأسمائها + **الأهداف من صفحة «للمعلم»** + **أوزانها** (الباب ٢ = ١٤ ص، الباب ١٣ = ٣ ص)
- **إرشادات المؤلف تُعرض للمعلم** عند فتح الباب ⟵ **المنصة مطبِّقة للمنهج لا دفتر رصد**
- **شاشة «السلم البياني»** — نعيد إنتاج درج المؤلف (ص ١٠) رقمياً
- الباب: تتبّعٌ **بلا بوابة** (`startedAt` + `completedAt` + `attendanceDays`)
- **٤ محطات + التخرج** — اختبار + اعتماد
- **م١**: المُختبِر ليس معلمه
- الترميم / العودة للصفر ⟵ **إعداد**، القيمة المبدئية `RESET_TO_ZERO`

**معيار القبول:**
- شريط التقدّم **بالوزن لا بالعدد** (اختبار: الباب ١٣ ≠ ٧٫٧٪)
- محاولة إسناد المعلم مُختبِراً لطالبه ← **يُرفض في الخادم** (اختبار)
- لا مُسمِّع متاح ← **تنبيه المدير**، لا صمت
- تغيير `MILESTONE_FAILURE_ACTION` من الشاشة ← **ينفذ بلا نشر**

**⚠️ حاجب:** الصفحات ٦–١١ من الكتاب (منهجية المؤلف) **تُقرأ مع محمد** قبل بذر البيانات.

---

### م٤ — مراقي

**أكبر مرحلة. تُجزّأ إلى أربع.**

#### م٤أ — المراحل والمسارات
- بذر ٦ مراحل + **٦٠ فرعية** ⟵ **يحتاج (ب)**
- **الفاتحة وحدة تمهيدية خارج الستين**
- `Track` **جدولاً** (٨ مسارات) — يُزاد من الإعدادات
- `PacePassage` — **بنك مقاطع**، اختيار عشوائي، والمنصة **تعايرها بالبيانات**
- `PaceTest` — **العدّاد عند المعلم، لا يظهر للطالب (م٦)**
- قاعدة الإسناد: **أعلى مسارٍ أقلُّ ممّا حفظ**
- `< ٣ أسطر` ← حالة `PACE_RETEST_SCHEDULED` **بموعدٍ مربوط**

**قبول:** كل مرحلة فرعية تُعرض **بالسورة والآية** • **كلمة «حزب» لا تظهر للطالب أبداً** • حفظ صفحة ← نصف صفحة

#### م٤ب — الجلسة اليومية
- `DailySession`: **حفظ** (المعلم وحده — **م٢**) + **ترسيخ** + **مراجعة**
- **الحفظ:** النطاق + **عدد المحاولات** + أتقن
- **الترسيخ:** نافذة **٩ أيام دوام** ⟵ **تمّ/لم يتم فقط**
- **المراجعة:** **خُمس المخزون يومياً** (الأسبوع ٥ أيام) ⟵ تمّ/لم يتم
- العريف: **نظراً** • **لا حصاد (م٣)** • زرّ **«أشِر للمعلم»**

**قبول:**
- **جدول الترسيخ يطابق مثال محمد حرفاً** — اليوم ١١ يُسقط الفاتحة، والثاني عشر يُسقط الناس (**اختبار**)
- **أيام دوام لا تقويم** (اختبار: عطلة لا تُحرّك النافذة)
- عريف يحاول دخول حصاد ← **يُرفض في الخادم**
- معلمٌ غير معلمه يحاول تسجيل حفظه ← **يُرفض**

#### م٤ج — الحصاد
- `Hasad` + `HasadPageError` (**بموضع الصفحة**)
- **خوارزمية الحدّ:**
  ```
  أي صفحة فيها خطآن ← راسب (توقّف)
  كل حزب ≤ خطأ      ← متميّز
  وإلا               ← ناجح
  ```
- **محرّك الإسناد**: ليس معلمه (م١) **عبر كل حلقاته** • ليس عريفاً (م٣) • موافقة الجنس • توزيع الحِمل • **تنبيه إن لم يوجد**
- **المعلم يعلن الجاهزية** ⟵ ولوحة المدير: *«أتمّوا ولم تُعلَن جاهزيتهم منذ كذا»*
- **يتوقف عن الحفظ الجديد** انتظاراً
- **الترميم الموضعي** ⟵ رسوبٌ ثانٍ ⟵ **العودة للصفر**
- **خريطة مواضع الضعف** — أي صفحات القرآن ترسّب طلابك أكثر

**قبول:**
- خطآن في صفحة واحدة و٥٩ نظيفة ← **راسب** (اختبار)
- صفحة نظيفة **لا تشفع** لما بعدها (اختبار: «لا تُرحَّل»)
- حصاد فرعية = **من أول المرحلة الأصلية** لا من أول البرنامج (اختبار)
- رسوب أول ← **الصفحات الراسبة معروضة** + الحفظ متوقف. رسوب ثانٍ ← الصفر.
- **عدّاد نسبة الرسوب في لوحة المدير** — لأن البرنامج قائمٌ على رقمٍ لا يملكه أحد اليوم

#### م٤د — الترقية
- **المناط: الاعتياد** = **المحاولة الأولى في الحفظ** + **إتمام الترسيخ والمراجعة** ← **الشرطان معاً**
- **المدة: حدٌّ أدنى ≈ ٥ أسابيع** — لأن الحِمل يتضاعف بعد **٩ أيام** لا يوم الترقية
- **الثلاثة أشهر: سؤالٌ لا أمر**
- **الخفض موجود** — سلّمٌ يصعد ولا ينزل **مصيدة**
- **المساندة:** المنصة **تشرح ولا تحكم** • **تدفع المتردّد** • المدير **يرى البيان لا التوقيع**
- **ولا نقارن المعلمين** — من يُرتَّب يتعلّم أن يُخفي

**قبول:**
- طالبٌ يُتقن من أول محاولة **ولا يُتمّ ترسيخه** ← **لا يُرشَّح** (اختبار — وإلا أغرقناه)
- شاشة الترقية تعرض **البيان لا الحكم**، ومعه: ⚠️ *«تضاعف جلسته من ١٠ إلى ٢٠ صفحة»*
- **تنبيه السعة الاستشرافي:** *«حلقة فلان ستتجاوز ما بين الأذانين بعد الترقيات القادمة»*

---

### م٥ — الشهادات

**يحتاج (أ).**

- **٤ قوالب** • التميّز **وسامٌ لا قالب** (فيبقى في الشهادة خبران: ما بلغتَ، وكيف)
- **PNG + QR**
- **صفحة تحقّق عامة** — **رمز عشوائي غير متسلسل**
- الإرسال: لولي الأمر إن كان دون ١٣

**قبول:**
- الاسم العربي **موصولٌ مشكولٌ صحيح** على Vercel
- `/verify/2` ← **لا يعرض شيئاً** (اختبار — الرمز غير قابل للتخمين)
- الصفحة تعرض: الاسم، الشهادة، التاريخ، صحّتها. **لا شيء غيرها** — لا هوية، لا جوال (**م٥**)

---

### م٦ — الاقتصاد ⏸️ بانتظار الشرح

**نقاط • متجر • بورصة إنجاز • مدفوعات • راعٍ (الدور الثامن الذي فاتني).**

**ولا يعطّل ما قبله**، لأنه **مستهلكٌ لأحداث القلب لا شريكٌ فيها**:

> أتقن ← **حدث** ← نقاط
> اجتاز متميّزاً ← **حدث** ← نقاط

**ومصمّم منصتك رأى هذا قبلي:** `points ( value, reason, source_event )`.

**⟵ ما دام القلب يُصدِر الأحداث (قاعدة ٥)، فالاقتصاد يشتري منها متى شئنا بلا هجرة.**

*(**المدفوعات والرعاة** مجالٌ آخر: مالٌ وجهاتٌ خارجية وبوابة دفع. التقرير: **٣٥٪ — سجلٌّ بلا بوابة**. تُصمَّم على حدة.)*

---

### م٧ — البرنامج الأسبوعي ⏸️ مؤجَّل بقرار

**آلية أخرى بالكلية** (قرار ٥٠). ⟵ **يدخل خلف حدّ البرنامج بآلته، بلا مساس بما بُني.**

---

## ٤. قالب برومبت كلود كود — لكل مرحلة

```
# المرحلة [رقم]: [الاسم]

## المرجع
اقرأ DESIGN.md كاملاً قبل أن تكتب سطراً. هو مصدر الحقيقة.
البنود التي تخصك: §[...]

## الحدود — لا تتجاوزها
- لا تلمس ملفاً خارج نطاق هذه المرحلة.
- لا تعدّل مخططاً موجوداً — أضف ترحيلاً جديداً.
- لا SQL يدوي. Prisma فقط.
- لا شاشة بلا مسار حفظ حقيقي. إن لم تكتمل، لا تبنِها.
- لا بيانات وهمية. لا mock. لا hardcoded.

## القواعد المطلقة التي تلمسها هذه المرحلة
[انسخها من DESIGN.md §٢]
لكل قاعدة: اختبارٌ يثبت أن تجاوزها **من العميل** يُرفض.
الحماية في الواجهة لا تُحتسب.

## معيار القبول
[انسخه من BUILD_PLAN.md]

## قبل كل commit
npx tsc --noEmit && npm run lint && npm run build
الثلاثة خضراء أو لا commit.

## عند الشك
توقّف واسأل. لا تخمّن قاعدة عمل.
ولا تكتب آية ولا حدّ حزبٍ من ذاكرتك — إن احتجته فاطلبه.
```

---

## ٥. لوحة المخاطر

| # | الخطر | الحال |
|---|---|---|
| 🔴 ١ | **RLS المفتوحة** (`p_all using(true)`) | تسقط في **م٠** بالبنية • **تُفحص قبل أول طالب** |
| 🔴 ٢ | **حدود الأحزاب الستين** | **(ب)** — بتوقيع محمد |
| 🔴 ٣ | **PNG عربية على Vercel** | **(أ)** — spike قبل م٥ |
| 🔴 ٤ | **رقم هوية القُصّر** | م١ — تشفير + حجب + سجل |
| 🟠 ٥ | **حقول الجهة الإشرافية** | **(ج)** — **لا تُستدرَك** |
| 🟠 ٦ | **الاستضافة داخل المملكة؟** | **(د)** — يُسأل الآن |
| 🟡 ٧ | **طاقة المُسمِّعين** | م١ بلا طاقة = **طابور انتظار** لا ضمانة نزاهة ⟵ مؤشر |
| 🟡 ٨ | **الحلقة تمتلئ بلا أن يدخلها أحد** | الترقية تملؤها من الداخل ⟵ تنبيه استشرافي |
| 🟡 ٩ | **تغطية التجويد < ١٠٪** | حلٌّ وسط معروض (DESIGN §٨٫٤) |
| 🟡 ١٠ | **نسبة الرسوب في الحصاد** | ٠٫٥٪ ← ٧٤٪ يختمون • ٥٪ ← ٥٪ ⟵ **عدّاد لا تخمين** |

---

## ٦. الترتيب — ابدأ من هنا

| # | الفعل | من | يعطّل |
|---|---|---|---|
| **١** | **فحص RLS بـcurl** | محمد | أول طالب حقيقي |
| **٢** | **سؤال الجهة عن الاستضافة** | محمد | **البنية التحتية كلها** |
| **٣** | **ملف الإكسل** | محمد | إغلاق المخطط |
| **٤** | **م٠ — الأساس والأمان** | كلود كود | كل شيء |
| **٥** | **(أ) spike الشهادة** | كلود كود | م٥ |
| **٦** | **(ب) حدود الأحزاب** | محمد | م٤ |
| **٧** | **شرح الاقتصاد** | محمد | م٦ فقط |

---

## ٧. المبادئ التي حكمت هذه الخطة

**١ — الأمان قبل الميزة.** بيانات القُصّر مسؤولية نظامية، لا بند في backlog.

**٢ — الأبسط أولاً.** القاعدة المدنية قبل مراقي — **يُثبت المحرّكات على مثالٍ يحتمل الخطأ**.

**٣ — الأنواع عينا الوكيل.** بلا `tsc`، كلود كود يبني معصوب العينين — **وأنت المُصحِّح الوحيد**، وتكتشف الكسر بعد أسبوع من مستخدم.

**٤ — لا شاشة وهمية.** ما يُوهم الاكتمال أخطر من غيابه. **وقد وقع مرة — ثمانِ شاشاتٍ حمراء.**

**٥ — الحدود تُغني عن التنبؤ.** حدّ **البرنامج** عزل الأسبوعي، وحدّ **الحدث** عزل الاقتصاد. **مبدأٌ واحد أنقذنا مرتين.**

**٦ — الإعداد لا الثابت** حيث لا دليل. تبدأ بما ترى، ثم تضبط بما وقع.

**٧ — لا نكتب من الذاكرة ما يُقرأ من مصدر.** آيةً، أو حدَّ حزب، أو مكدَّس مشروع.
**وهذا الأخير تعلّمتُه في هذه الجلسة:** زعمتُ أن مشروعك Next.js لأني تذكّرتُ، لا لأني قرأت. **والتقرير كذّبني.**

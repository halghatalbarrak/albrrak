// ═══════════════ ترتيبٌ عالميٌّ للآيات (مرجعٌ ثابت) ═══════════════
//
// عدد آيات كل سورة بترتيب المصحف (عدّ الكوفة، المجموع ٦٢٣٦). مرجعٌ قانونيٌّ ثابت لا
// يُشتقّ من غيره — يُمكّن من قياس «تغطية حزبٍ كامل» بحدود السور (تقاطعًا مع HizbBoundary،
// الحكم ٨): تحويل أيّ (سورة، آية) إلى موضعٍ عالميٍّ واحدٍ فتُقارَن المقاطع بحدود الأحزاب.

/** عدد آيات كل سورة، مفهرسًا من ١ (الفهرس ٠ حشوٌ ليطابق رقم السورة). */
export const SURAH_AYAH_COUNTS: readonly number[] = [
  0,
  7, 286, 200, 176, 120, 165, 206, 75, 129, 109, 123, 111, 43, 52, 99, 128, 111, 110, 98, 135,
  112, 78, 118, 64, 77, 227, 93, 88, 69, 60, 34, 30, 73, 54, 45, 83, 182, 88, 75, 85,
  54, 53, 89, 59, 37, 35, 38, 29, 18, 45, 60, 49, 62, 55, 78, 96, 29, 22, 24, 13,
  14, 11, 11, 18, 12, 12, 30, 52, 52, 44, 28, 28, 20, 56, 40, 31, 50, 40, 46, 42,
  29, 19, 36, 25, 22, 17, 19, 26, 30, 20, 15, 21, 11, 8, 8, 19, 5, 8, 8, 11,
  11, 8, 3, 9, 5, 4, 7, 3, 6, 3, 5, 4, 5, 6,
];

// مجاميع تراكميّة لبداية كل سورة: CUMULATIVE[surah] = عدد الآيات قبل بدايتها (تُحسب مرّةً).
const CUMULATIVE: readonly number[] = (() => {
  const c = [0, 0];
  for (let s = 1; s <= 114; s++) c[s + 1] = c[s] + SURAH_AYAH_COUNTS[s];
  return c;
})();

/** موضع الآية العالميّ (١..٦٢٣٦): مجموع آيات ما قبل السورة + رقم الآية. */
export function ayahOrdinal(surah: number, ayah: number): number {
  return (CUMULATIVE[surah] ?? 0) + ayah;
}

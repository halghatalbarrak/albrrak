# التسكين والانتقال بين البرامج — قواعد ملزِمة (المهمّة ٦)

> **المصدر:** تصوّرٌ أقرّه محمد (مدير الحلقات) بالحوار. هذه القواعد تُلزِم الكود، لا تُخترَع.
> **مرجعٌ ملزم — يُقرأ قبل أيّ تعديل يمسّ برنامج الطالب أو تسكينه أو انتقاله.**
>
> مرتبطٌ بـ: [ROLES.md](ROLES.md) (الصلاحيّات) · [MARAQI_RULES.md](MARAQI_RULES.md) (وجهة اليوم والجبهة) · [QAIDAH_RULES.md](QAIDAH_RULES.md) (تخرّج القاعدة وقفل ق٧) · [HANDOFF.md](HANDOFF.md) (مزامنة الإنتاج والفهرس الجزئيّ).

---

## المبدأ

البرنامج **صفةٌ للطالب في قيده (Enrollment)**، لا للحلقة. الحلقة قد تجمع طلاباً من برامج مختلفة، ولها **برنامجٌ افتراضيّ** يُسنَد للمنضمّ الجديد ما لم يُحدَّد غيره. الانتقال بين البرامج مسارٌ مُدارٌ من الإدارة، لا أسماء محفورة في المنطق.

---

## القرارات ق١–ق٧

### ق١ — البرنامج صفةٌ في القيد
- `Enrollment.programId` هو مصدر الحقيقة لبرنامج الطالب (لا `Circle.programId`).
- الحلقة تجمع برامج؛ ولها **برنامجٌ افتراضيّ** (`Circle.programId`، يبقى باسمه ودلالته «الافتراضيّ» — لا إعادة تسمية، حفاظاً على التوافق) يُسنَد للمنضمّ الجديد إن لم يُحدَّد له برنامج.
- **كلّ إنشاء قيدٍ يكتب `programId`** (لا قيدٌ بلا برنامج): الإسناد الإداريّ (`enrollStudent`) والاختبار (`decideReadingTest`) والانتقال (ق٥). المُوصِّل `activeCircle` يقرأ البرنامج **من القيد**، مع ارتدادٍ دفاعيٍّ لبرنامج الحلقة الافتراضيّ إن لم يُبذَر بعد.

#### اختبار القراءة (`placement.ts`) — مربوطٌ بالنموذج الجديد
`decideReadingTest` (اعتماد المدير، §٦٫٣): «لا يجيد نظراً» ⟵ يُنشئ قيد القاعدة **ويكتب `Enrollment.programId`** (برنامج حلقة القاعدة المُتحقَّق منها)، **ويُسجَّل في `ProgramEnrollmentHistory` بسبب `READING_TEST`**. «يجيد نظراً» ⟵ لا قيد الآن (اختبار المقطع يليه). فلا مصدرين للبرنامج. الصلاحيّة صلاحيّة اعتماد الاختبار (المدير/المشرف) — متوافقةٌ مع ق٧.

### ق٢ — تسكين القاعدة المدنية: اختيار الدرس الحاليّ
- المسكِّن يختار **الدرس الحاليّ** للطالب من قائمة دروس القاعدة.
- كلّ الدروس **قبله** تُعلَّم `COMPLETED` بمصدر **`PLACEMENT`** — مميّزةٌ عن الإتقان الفعليّ (مصدرٌ مختلف)، **وبلا نقاط**.

### ق٣ — تسكين مراقي: تأشير المحفوظ
- المسكِّن يؤشّر المحفوظ: **أجزاء كاملة** + **موضع الوصول (سورة:آية)** في الجزء الحاليّ + **محفوظ خارج الترتيب** (أجزاءً أو سوراً مفردة).
- النظام **يستنتج الجبهة** = أوّل آيةٍ غير محفوظة في **ترتيب مراقي** (تنازليّ: الفاتحة ثمّ الناس صعوداً إلى البقرة).
- **كلّ المحفوظ الراسخ يدخل المراجعة فوراً.**
- **الحفظ الجديد يتخطّى المحفوظ خارج الترتيب** حين يبلغه في تقدّمه.
- **بلا نقاط.**

### ق٤ — مسار الانتقال يُدار من الإدارة
- `Program.nextProgram` (علاقة `nextProgramId`) تُدار من إعدادات البرنامج: القاعدة ← مراقي، ومراقي ← «المُدَّكِر» لاحقاً.
- **لا أسماء برامج محفورة في منطق الانتقال** — المنطق يقرأ `nextProgramId`.

### ق٥ — الانتقال عند تخرّج القاعدة
- عند `QAIDAH_COMPLETE`: ينتقل الطالب إلى `nextProgram` **ابتداءً من اليوم التالي**.
- **يوم التخرّج يبقى قاعدة** (وقفل ق٧ من دمج القاعدة السابق **ساري**: لا تقييمَ قاعدةٍ بعد تخرّجٍ سبّبه تقييم اليوم).
- يبدأ في البرنامج الجديد **من الفاتحة** بمسار `Program.defaultTrackForIncoming` (يُضبط من إعدادات مراقي)، **والمعلّم يغيّر المسار** بعد ذلك.
- **إن لم يُضبط `nextProgram` أو المسار الافتراضيّ ⟵ لا انتقال، وتنبيهٌ للمشرف** (لا فشلٌ صامت).

### ق٦ — حريّة التسكين وتسجيله
- التسكين **حرٌّ حتى أوّل جلسةٍ فعليّة** في البرنامج (تعديلٌ بلا قيد).
- **بعد أوّل جلسة**: يُسمَح بالتعديل **ويُسجَّل حدثاً** (من، متى، القيمة قبل/بعد).
- **تاريخ البرامج محفوظ**: دخل/خرج، والسبب (إسناد / تخرّج / تغيير).

### ق٧ — الصلاحيّات (تُطابق [ROLES.md](ROLES.md))
- **تغيير البرنامج والتسكين:** المشرف العام (`SUPER_ADMIN`) ومدير الحلقة (`CIRCLE_MANAGER`) والمدير التقنيّ (`TECH_ADMIN`، القمّة المطلقة).
- **تغيير المسار (Track):** المعلّم (`TEACHER`) أيضاً — إضافةً إلى الثلاثة أعلاه.
- القيود **في الخادم** (لا الواجهة)، مع اختبارات تُثبت الرفض.

---

## المخطّط (إضافيٌّ ومتوافق — ترحيلٌ واحد)

- `Enrollment.programId` (+ ترحيل بيانات: كل قيدٍ قائم يأخذ برنامج حلقته).
- `Circle.programId` **يبقى** (دلالته الآن «الافتراضيّ للمنضمّ الجديد»).
- `Program.nextProgramId` و`Program.defaultTrackForIncomingId` (كلاهما nullable).
- إتقان دروس القاعدة بمصدر `PLACEMENT` (مميّزٌ عن الإتقان الفعليّ).
- تخزين محفوظ مراقي المسكَّن (نطاقات آيات) بشكلٍ تقرؤه وجهة اليوم.
- جدول **تاريخ البرامج** (`ProgramEnrollmentHistory`): دخل/خرج/السبب/الفاعل.
- **تنبيه:** الفهرس الفريد الجزئيّ على `AutoGrant` مكتوبٌ يدويّاً (راجع HANDOFF)؛ إن ولّد Prisma ما يحذفه فيُحذف ذلك من الترحيل.

---

## الأثر على المحرّكات (سارية بلا نقض)

- **وجهة اليوم (مراقي):** الجبهة تُحسب من **التسكين + الجلسات معاً**؛ المراجعة تشمل المحفوظ المسكَّن الراسخ؛ الحفظ الجديد يتخطّى المحفوظ خارج الترتيب. كلّ قرارات [MARAQI_RULES.md](MARAQI_RULES.md) سارية.
- **الشاشة الموحّدة:** في حلقةٍ مختلطة البرامج، تعرض لكلّ طالبٍ **لوحة برنامجه هو** (المقروء من قيده).
- **الانتقال الآليّ:** داخل معاملة التخرّج نفسها (ق٥).

# منصة حلقات البراك — وثيقة التسليم

> **الغرض:** نقل السياق كاملاً إلى محادثة جديدة. اقرأها أولاً قبل أي عمل.

---

## من أنا ومن أنت

**المستخدم:** محمد — مدير حلقات قرآنية في المدينة المنورة. **غير مبرمج.** يفوّض القرارات التقنية، ويحتفظ بالقرارات **الدينية والتربوية والمنهجية**.

**أسلوب التعامل المطلوب:**
- **«الزبدة»** — إجابات موجزة، بلا إطالة
- **خطوة واحدة كل مرة** — لا قوائم طويلة
- الشرح بالعربية، وبلغة غير المبرمج
- **لا تفترض معرفةً تقنية** — اشرح أين يضغط وماذا يرى

**دور المساعد:** المدير التقني. **البناء الفعليّ يتم عبر وكيل Claude Code على جهاز محمد**، والمساعد يراجع تقاريره ويوجّهه ببرومبتات جاهزة للّصق.

---

## البنية التحتية — معرّفات حرفية

| | |
|---|---|
| **GitHub** | `github.com/halghatalbarrak/albrrak` |
| **الجهاز** | `D:\PROGRAM\albrrak` (Windows) |
| **Supabase** | مشروع `barrak-v2`، فرانكفورت `eu-central-1`، ref `rsrsngtjbeschsslpklh` |
| **المضيف** | `aws-0-eu-central-1.pooler.supabase.com` (6543 للتطبيق، 5432 للترحيلات) |
| **Vercel** | `albrrak.vercel.app` — حساب `albarrak11` |
| **المكدَّس** | Next.js + TypeScript + Prisma + Supabase + Vercel |

### ⚠️ فخاخ معروفة (وقعنا فيها فعلاً)
- **ثلاثة حسابات GitHub على الجهاز** (`mghniman`, `alameen5509`, `albtaqtalsyahyt-sketch`) — لا يملك أيّها المستودع. الدفع يحتاج `gh auth login` بـ`halghatalbarrak`.
- **محرّر محمد لا يحفظ `.env`** — يعيد نسخة مخزّنة قديمة. **استعمل PowerShell أو Notepad فقط.**
- **مشروع Supabase يُوقَف** بعد أسبوع خمول → `Restore`.
- **GitHub Actions معطّل** — يقف في الطابور ثم يُلغى، والسبب **لم يُشخَّص** (المستودع عام فالدقائق ليست القيد؛ فُحصت الفوترة والصلاحيات والتزامن). **لا تعتمد عليه.** الاختبار صار محليًّا بـembedded-postgres (القسم أدناه).
- الوكيل **يخلط بين ذاكرته وحالة GitHub/الإنتاج** → تحقّق دائماً بـ`git`/`migrate status`/صور المتصفّح.

---

## الحالة الآن (حيّ على الإنتاج)

### مكتمل ومنشور
- **م٠** الأساس والأمان (RLS، JWKS/ES256، تشفير رقم الهوية)
- **م١** الناس والمحرّكات (قيد ← قبول ← حساب ← إسناد ← قوائم)
- **م٢** الحضور (ست حالات، بلا إنترنت، مواقيت المدينة محسوبة، تقويم أم القرى)
- **م٣** القاعدة المدنية (١٣ باباً + تمهيد، بأهداف الكتاب ونصّ المؤلف)
- **م٤أ** مراقي (٦٠ حزباً، ترتيب تنازليّ)
- **م٤ب** الجلسة اليومية
- **م٤ج** الحصاد (أساس + خوارزمية الحدّ)
- **م٤د** انتقال الحزب التلقائيّ
- **الواجهة** لوحة حسب الدور + شاشتا السلّم البياني ومراقي
- **مولّد الشهادة** (Chromium + puppeteer، عربية سليمة) — دورمنت لـم٥

### أحكام مراقي المنفَّذة
**التسعة كلها (١–٩) مكتملةٌ ومنشورةٌ على الإنتاج.** والحكم ٧ **كاملٌ بمراحله الثماني** (محرّك التقدير + خريطة الأوجه + ربط الحصاد + اختبار المرحلة + اعتماد المدير + التخرّج + الأصول + شاشة الحصاد).

- **الطبعة المعتمدة: مصحف المدينة ١٤٤١** (quran-svg) — مصدرٌ واحد: الصورة والمضلّعات وخريطة `MushafFace`.
- **أصول المصحف على Supabase Storage:** bucket `mushaf` عامّ القراءة (لا كتابة عامّة)، **٦٠٤ WebP + ٦٠٤ مضلّعات JSON = ٩٫٨MB**. القاعدة في `MUSHAF_ASSETS_BASE` (.env + Vercel).
- شاشة الحصاد: `/admin/hasad` (صورة الوجه + تظليل الآية + خطأ/تردّد + المرتبة اللحظيّة).

**main عند `48bd4f9`. القاعدة عند migration 19 (MushafFace طبعة ١٤٤١). جداول الحكم ٧ على الإنتاج: `MushafFace`(٦٠٤)، `HasadHesitation`، `StageExam`.**

---

## أحكام مراقي التسعة (مرجع ملزِم)

> الملف الكامل: `MARAQI_RULES.md` في جذر المشروع. **الوكيل يقرأ منه، لا من الذاكرة.**

| # | الحكم |
|---|---|
| **١** | **الفرص الثلاث:** أي خطأ = رسوب المحاولة. ثلاث محاولات (يراجع في الجلسة بين كلٍّ)، ثم يعيد نفس الحفظ في يوم الحلقة التالي بلا جديد. لا حفظ جديد قبل إتقان حفظ اليوم. |
| **٢** | **الترسيخ:** نافذة = **آخر ١٠ جلسات حفظ**. يُراجَع يومياً، ثم يخرج راسخاً. |
| **٣** | **أيام الحلقة:** الأحد → الخميس. **الجمعة والسبت لا تُحسب إطلاقاً** في أي عدّ زمنيّ. |
| **٤** | **المراجعة الأسبوعية:** كامل المحفوظ الراسخ ÷ ٥ أيام (خُمس/يوم مقترح). الطالب حرّ في التعجيل، المهم إتمام الدورة أسبوعياً. **تتبّع دقيق للمقدار** (مطلوب/منجَز/متبقٍّ). |
| **٥** | **الترميم:** خطأ واحد في المراجعة = تنبيه. **خطآن في المقطع** → يعود «حفظاً جديداً» من أول الدورة. **العدّ تراكميّ على المقطع داخل نافذة آخر ١٠ جلسات حفظ** (لا داخل الجلسة الواحدة — قرار محمد): خطأ قديم خرج من النافذة لا يُحتسب، ويُحفظ سجلّ أخطاء لكل مقطع (تاريخ ومَن رصد). |
| **٦** | **الإسناد:** الحفظ الجديد **للمعلّم حصراً**. الترسيخ والمراجعة: الأصل المعلّم، **ويجوز إسنادها لأي شخص** بتوثيق ومسؤولية المعلّم. **التسميع ≠ الاختبار** — الحياد (المُختبِر ليس معلمه) للاختبار فقط. |
| **٧** | **الانتقال:** الحزب ينتقل **بحصاده فقط** (بلا اعتماد). **المرحلة الأصلية:** اختبار محايد (كامل المحفوظ، جلساتٌ بعدد الأحزاب) → اعتماد المدير → انتقال. **التخرّج:** المرحلة السادسة ٣ جولاتٍ ناجحة بينها شهرٌ ميلاديّ → اعتماد → شهادة KHATM. **الحصاد:** صورة الوجه + تظليل الآية + عدّ تراكميّ على الحزب + تردّد (٣/وجه=خطأ) + مراتب (تميّز≤١/اجتياز≤٥/رسوب≥٦). **مكتملٌ ومنشور.** |
| **٨** | **العريف:** طالب متقدّم يُسمِّع لزملائه (ترسيخ/مراجعة فقط). لا يُسمِّع الحفظ الجديد، ولا يختبر. **الأهلية: نفس الحلقة + تعيين المعلّم + راسخٌ ≥ حزب كامل** (شرطٌ آليٌّ مانع — تغطيةً بتقاطع حدود الأحزاب). **الشرط دائم لا للتعيين فقط:** من نزل راسخه تحت حزب (بترميم الحكم ٥) **يُعزل آليًّا لحظة الترميم** ويُعلَم المعلّم برسالة. |
| **٩** | **مستوى المقطع:** المقطع = **جلسة حفظ** بمقدار متدرّج (٣ أسطر · ٥ أسطر · نصف وجه · وجه · وجهان ... حتى ١٠ أوجه). يُحدَّد بـ**اختبار نصف ساعة** (يُعطى المقدار الأقلّ مما حفظ). **ثابت ٤ أشهر، يرتفع بموافقة المعلّم.** |

---

## الخطوة التالية مباشرةً

**الحكم ٧ اكتمل ومنشور.** التالي على الطاولة: **م٥ الشهادات** (مولّد الشهادات جاهزٌ دورمنت — يحتاج القوالب والتحقّق والتخزين والمشاركة). وشهادة KHATM تُصدَر آليًّا عند اعتماد التخرّج (الحكم ٨).

```
استطلع مولّد الشهادات (certificate.ts) وشهادة KHATM: ما الجاهز؟ وما الناقص
(قوالب/تحقّق/تخزين/مشاركة)؟ ما يحتاج ترحيلاً؟ لخّص الفجوة وتوقّف لإذن محمد.
```

---

## المتبقّي بعد ذلك

| # | البند | يحتاج |
|---|---|---|
| ١ | **م٥ الشهادات** | مولّد جاهز (دورمنت)؛ يحتاج القوالب والتحقّق والتخزين والمشاركة. **الخطوة التالية.** |
| ٢ | **الإطار الأخضر لصور الأوجه** | تحسينٌ مؤجَّلٌ باتّفاق محمد (الصور أحاديّة اللون الآن) |
| ٣ | **محرّك إسناد المُسمِّع الموسّع** | الأساس مبنيّ (تسميع مرن الحكم ٦ + العريف الحكم ٨)؛ يبقى التوسيع إن لزم |
| ٤ | **م٦ الاقتصاد** | 🔴 **شرح محمد** — نقاط، متجر، بورصة، راعٍ. **لم يُشرح بعد.** |
| ٥ | **م٧ الأسبوعي** | مؤجَّل |

### 🔴 أمانة مؤجّلة
**كلمة سرّ قاعدة البيانات ظهرت في محادثات سابقة** → يجب تجديدها (Supabase Reset) وتحديثها في: `.env` (عبر PowerShell)، GitHub Secrets، Vercel. **محمد أجّلها حتى اكتمال المشروع.**

---

## الاختبارات والترحيل — الطريق الأساسيّ الآن

**بديل GitHub Actions المعطّل — صار هو الطريق الأساسيّ لا البديل.**

- **`npm test` وحده** يُشعِل Postgres مؤقّتًا على الجهاز تلقائيًّا (`embedded-postgres`)، يطبّق كلّ الترحيلات عليه، يشغّل **٢٠٨ اختبارًا** (٣٣ ملفًّا)، ثم يُطفئه ويمسحه. **بلا Docker ولا WSL ولا إنترنت.** غير المبرمج: يفتح Terminal، يكتب `npm test`، ينتظر `passed` بالأخضر.
- الإعداد في `vitest.global-setup.ts` + `vitest.config.ts`؛ صمّام أمان في `testing/helpers.ts` يرفض أي `DATABASE_URL` لا يبدو محليًّا (حمايةٌ للإنتاج).
- **الترحيلات على الإنتاج من PowerShell:** `npx prisma migrate deploy` (يقرأ `.env`) — **deploy فقط، لا `db push` ولا `reset` ولا `migrate dev`.**
- التحقّق بعد الترحيل: `npx prisma migrate status` (يجب «up to date»)، واستعلامٌ مباشرٌ عبر `DIRECT_URL` للتأكّد من وجود الجدول فعليًّا.
- **فحوص GitHub في الـPR تُتجاهَل** (معلّقة/فاشلة لأن CI معطّل) — **الاختبار المحليّ هو الحكم.**

---

## الطريق المجرَّب للنشر

```
١. الوكيل يبني على فرع باسم المهمة (مثل maraqi/arif)
٢. npm test (embedded-postgres، ٢٠٨) + tsc + lint + build — كلها محليًّا
٣. commit + push → PR إلى main
٤. ادمج PR (محمد Admin؛ فحوص GitHub تُتجاهَل — الاختبار المحليّ هو الحكم)
٥. إن وُجد ترحيل: طبّقه من PowerShell — npx prisma migrate deploy (deploy فقط)
٦. Vercel ينشر main تلقائياً
```

**⚠️ قاعدة ثابتة:** أي ترحيل طُبِّق أو دُمج **لا يُعدَّل — يُضاف جديد**. والترحيلات تُطبَّق **معجميًّا** (0,1,10,11,...,2,3) فليكن كلٌّ مستقلًّا.

---

## مبادئ لا تُخالَف

1. **لا يُكتب من الذاكرة ما يُقرأ من مصدر** — آية، حدّ حزب، هدف باب، قيمة جهة. الوكيل يتوقّف ويسأل بدل أن يخترع، وهذا **مطلوب لا عيب**.
2. **القرارات الدينية/التربوية لمحمد وحده.** المساعد يسأل، يوضّح الخيارات، يوصي — **ولا يقرّر.**
3. **تحقّق من الواقع** (`git status`, `migrate status`, صور المتصفّح) — **لا من وصف الوكيل** (ذاكرته تُضغط ويرتبك).
4. **المراجعة البشرية تكشف ما لا يكشفه الاختبار.** محمد أمسك خطأ عرض في مراقي كانت كل الاختبارات خضراء معه.
5. **دفعات صغيرة مختبَرة** — لا دفعة ضخمة تُخفي الأعطاب.
6. **الأسرار لا تمرّ بمحادثة.** أي سرّ ظهر = محروق يُدوَّر.
7. **كل مرحلة تُغلق مختبَرةً على الإنتاج**، وتُصوَّر شاشتها للتأكيد.

---

## ملفات مرجعية في المشروع

| الملف | المحتوى |
|---|---|
| `MARAQI_RULES.md` | الأحكام التسعة (مرجع البناء) |
| `DESIGN.md` | ٥٠ قراراً تصميمياً + القواعد المطلقة |
| `BUILD_PLAN.md` | المراحل ومعايير القبول |
| `civil_base_seed.json` | ١٤ وحدة القاعدة المدنية بأهدافها |
| `hizb_boundaries.json` | ٦٠ حزباً بحدودها (راجعها محمد ووقّعها) |

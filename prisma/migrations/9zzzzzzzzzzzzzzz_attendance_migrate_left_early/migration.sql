-- ترحيل «خرج مبكّراً» (LEFT_EARLY) القائم إلى «حاضر» (PRESENT) — قرار محمد: كانت تُحتسب
-- حضورًا في القياس، فلا يُعاقَب أحدٌ بأثرٍ رجعيّ. حمايةٌ للبيانات الحيّة (المرحلة أ). الاسم
-- «9zzzzzzzzzzzzzzz» (١٥ z) ليُطبَّق بعد إضافة القيمة الجديدة (معاملةٌ منفصلة). إضافةٌ آمنة.
UPDATE "Attendance" SET "status" = 'PRESENT' WHERE "status" = 'LEFT_EARLY';

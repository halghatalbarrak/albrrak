-- الحكم ٤ الموسّع: تتبّع مقدار المراجعة المُنجَز — عمودٌ إضافيّ اختياريّ على الجلسة.
-- يتراكم أسبوعيًّا (المُنجَز = مجموع المُسمَّع). الكود القديم يتجاهله بأمان.
-- (Prisma يطبّق معجميًّا: 12 يسبق 2، لكنه يعتمد DailySession من 0_init فقط — آمن.)
ALTER TABLE "DailySession" ADD COLUMN "murajaahCount" INTEGER;

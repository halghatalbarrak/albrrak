# تشخيص تقني شامل — منصة حلقات الشيخ محمد البراك

> **تقرير مدقّق مستقل — قراءة فقط.** لم يُعدَّل أي ملف من ملفات المشروع؛ هذا الملف هو المخرَج الوحيد.
> تاريخ التدقيق: ٢٠٢٦-٠٧-١٦ · الفرع المفحوص: `claude/market-ecommerce` (آخر commit: `0b9d0d2`).

---

## ⚠️ ملاحظة جوهرية قبل كل شيء: المكدّس التقني لا يطابق نموذج التشخيص المطلوب

نموذج الطلب افترض مشروع **Next.js + Prisma + TypeScript + Tailwind + Supabase**. الواقع مختلف تمامًا:

| المفترض في الطلب | الواقع الفعلي |
|---|---|
| Next.js (App/Pages Router) | **لا يوجد** — لا `next.config`، لا `.next`، لا SSR |
| React | **لا يوجد** — DOM خام عبر `innerHTML` |
| TypeScript | **لا يوجد** — JavaScript عادي (ES) بلا أنواع |
| Prisma | **لا يوجد** — المخطط ملفات SQL خام تُشغَّل يدويًا في Supabase |
| Tailwind | **لا يوجد** — CSS يدوي (`css/styles.css`, ٥٨٠ سطرًا) بمتغيّرات CSS |
| `package.json` / `node_modules` / خطوة بناء | **لا يوجد** — لا اعتماديات npm ولا build |

**الحقيقة:** المشروع تطبيق **صفحة واحدة ثابت (Static Vanilla SPA)** — ثلاثة ملفات JS + CSS + `index.html` واحد، يُنشر مباشرةً على Vercel كملفات ثابتة، ويتصل بـ Supabase عبر SDK من CDN. لا يوجد خادم ولا طبقة API خلفية إطلاقًا.

**النتيجة على التقرير:** الأقسام التي تطلب `tsc --noEmit` و`npm run lint` و`npm run build` و`npm audit` و`prisma migrate status` **غير قابلة للتطبيق** (لا وجود لأدواتها). سأوثّق ذلك صراحةً في كل قسم بدل اختلاق مخرجات.

---

## ١. الخريطة

### شجرة المجلدات (كامل المستودع — لا `node_modules` أصلًا)

```
.
├── index.html                 # القشرة (٩١ سطرًا) — تحمّل السكربتات فقط
├── css/
│   └── styles.css             # نظام التصميم كاملًا (٥٨٠ سطرًا، RTL، متغيّرات CSS)
├── js/
│   ├── app.js                 # كل منطق التطبيق والواجهة (١٤٠٨ أسطر)
│   ├── db.js                  # طبقة البيانات + المصادقة على Supabase (٢٨٠ سطرًا)
│   ├── config.js              # رابط Supabase + المفتاح العام (مرفوع في git)
│   └── config.example.js      # قالب الإعداد
├── sql/                       # ترحيلات لاحقة (تُشغَّل يدويًا بالترتيب)
│   ├── 01_add_profile_links.sql
│   ├── 02_admin_management.sql
│   ├── 03_enforce_disabled.sql
│   └── 04_teacher_applications_rls.sql
├── supabase/                  # المخطط الأساسي والسياسات
│   ├── schema.sql             # الجداول + بذور + RLS مفتوحة مبدئية
│   ├── auth_profiles.sql      # جدول profiles + سياسات مبدئية
│   ├── rls_policies.sql       # سياسات RLS الإنتاجية بالأدوار
│   └── market.sql             # كتالوج الجوائز + جدول orders + RLS
├── assets/favicon.svg
├── manifest.webmanifest       # PWA
├── sw.js                      # Service Worker (نسخة «ذاتية الإلغاء»)
├── vercel.json                # رؤوس أمنية + CSP + تخزين مؤقّت
├── version.txt                # بصمة إصدار
└── README.md
```

### الإصدارات

- **لا يوجد `package.json`** → لا إصدارات Node/Next/React/TS/Prisma/Tailwind.
- الاعتماد الخارجي الوحيد: **`@supabase/supabase-js@2.45.4`** مثبّت من `cdn.jsdelivr.net` في `index.html` (UMD في المتصفّح).
- الخطوط: `IBM Plex Sans Arabic` + `Amiri` من Google Fonts.
- التشغيل: Node متاح في بيئة التدقيق (`node --check` نجح على كل ملفات JS) لكن المشروع لا يستهلكه.

### App Router أم Pages Router؟ وتوزيع Server/Client

لا هذا ولا ذاك. **كل شيء Client-side.** لا مكوّنات خادم إطلاقًا. التوجيه (routing) **وهمي داخل المتصفّح**: متغيّر `VIEW` + دالة `switchTo()` + دالة `render()` التي تختار دالة عرض من خريطة (`app.js:205-217`). لا عناوين URL حقيقية للصفحات (كلها `/`)، ما عدا وضع واحد: `?apply` / `#apply` يفتح نموذج التقديم العام (`isApplyMode()`).

### جدول الشاشات (Views) والحالة الفعلية

لا توجد «صفحات» ولا «API endpoints» بالمعنى التقليدي. الشاشات = ٢٤ دالة عرض. التصنيف مبنيّ على فحص الكود: هل الشاشة تقرأ/تكتب بيانات حقيقية عبر `DB` (Supabase)، أم تعمل على ذاكرة محلية فقط، أم أرقامها ثابتة؟

| الشاشة | الوصف | الحالة |
|---|---|---|
| `tasmee` | التسميع اليومي | ✅ **حقيقي** — يرصد الجلسة ويحسب النقاط ويحفظ عبر `DB.saveSession` (طابور → Supabase) |
| `admissions` | القبول والانتظار | ✅ **حقيقي** — قبول/رفض يُحفظ عبر `DB.acceptStudent/updateWaitlist` |
| `market` | سوق الحلقة (المتجر) | ✅ **حقيقي** — سلّة/طلب يخصم النقاط والمخزون ويحفظ الطلب في `orders` |
| `users` | إدارة المستخدمين | ✅ **حقيقي** — يقرأ `profiles` ويغيّر الأدوار/الحالة عبر RPC (يتطلّب مشرفًا عامًّا) |
| `teacherapps` | طلبات المعلمين | ✅ **حقيقي** — جدول `teacher_applications` (إدراج/قراءة) |
| `circlemgmt` | إدارة الحلقات | ✅ **حقيقي** — `DB.saveCircle` (upsert) + كشف المعلمين |
| `studentmgmt` | إدارة الطلاب | ✅ **حقيقي** — `DB.saveStudent` (إدراج/تحديث) |
| `guardianmgmt` | إدارة العائلات | ✅ **حقيقي** — `DB.saveGuardian` (upsert) |
| `dash` | لوحة القيادة | 🟡 **محسوب من الذاكرة** — KPIs من مصفوفة `STUDENTS` (حقيقي إن حُمّلت من القاعدة) |
| `exchange` | بورصة الإنجاز | 🟡 **محسوب من الذاكرة** — عرض حيّ لحالة اليوم |
| `kiosk` | شاشة المسجد | 🟡 **محسوب من الذاكرة** — لوحات شرف من `STUDENTS` |
| `children` | أبناء ولي الأمر | 🟡 **قراءة** — يفلتر `STUDENTS` بـ `guardian_id` |
| `myprog` | برنامج الطالب | 🟡 **قراءة/حساب** — مستوى ونقاط الطالب الحالي |
| `reports` | التقارير | 🟠 **مختلط** — بعض المؤشرات محسوبة، وأخرى ثابتة (تفاعل ولي الأمر `ring(80)`، P95=١٢ث، زمن التحميل ١٫٤ث)؛ التصدير = `toast` فقط |
| `finance` | البوابة المالية | 🟠 **واجهة + سجل فقط** — يسجّل صفوف دفع في `payments`، **لا توجد بوابة دفع حقيقية** (Apple Pay/STC مذكورة نصًّا فقط) |
| `harvest` | جلسات الحصاد | 🟠 **ذاكرة فقط** — الاجتياز/الرسوب والنقاط في الذاكرة؛ يُوثّق في `audit` لكن حالة الحصاد لا تُحفظ في جدول `harvest` |
| `sheet` | ورقة المتابعة الشهرية | 🔴 **واجهة/بيانات مُلفّقة** — صفوف الأيام مولّدة حسابيًّا (`i%4===0?'red':'green'`) لا من جلسات حقيقية؛ «تصدير PDF» = `toast` |
| `notif` | الإشعارات | 🔴 **ذاكرة فقط** — مصفوفة `NOTIFS` محلية، «القراءة» في الذاكرة، والمصفوفة جدول ثابت؛ لا قناة إرسال فعلية |
| `linking` | الربط والمراجعة | 🔴 **واجهة فقط** — `adjustLink` = `toast` + توثيق؛ لا جدول ربط يُحفظ |
| `excuses` | الأعذار | 🔴 **ذاكرة فقط** — مصفوفة `EXCUSES` محلية، لا تُحفظ في القاعدة |
| `approvals` | الموافقات | 🔴 **ذاكرة فقط** — مصفوفة `APPROVALS` محلية (الدفع فقط يُسجَّل) |
| `qr` | سجل QR | 🔴 **وهمي** — «رمز QR» مصفوفة بتّات ثابتة مرسومة يدويًّا، «نسخ الرابط» = `toast` |
| `impact` | تقارير الأثر (الداعم) | 🔴 **أرقام ثابتة** — ٨٣٪/٧٨٪/٩١٪/٢٠٠٠ مكتوبة يدويًّا |
| `audit` | سجل التدقيق | 🟡 **قراءة** — يعرض `AUDIT` (يُحمَّل من `audit_log`) |

**طبقة الـ API:** لا يوجد Backend مخصّص. كل الوصول عبر Supabase مباشرةً: `.from()` على ١٤ جدولًا + **٣ دوال RPC**: `admin_set_role`, `admin_set_status`, `admin_link_user`.

---

## ٢. قاعدة البيانات

### `prisma/schema.prisma`

**لا وجود له** — لا Prisma في المشروع. المخطط مُعرّف في PostgreSQL خام موزّع على **٨ ملفات SQL** تُشغَّل يدويًا في Supabase SQL Editor بترتيب موثّق في التعليقات.

### ملخّص النماذج والعلاقات (من `supabase/schema.sql` + الترحيلات)

| الجدول | المفتاح | أبرز الحقول | العلاقات |
|---|---|---|---|
| `circles` | `text id` | name, mosque, teacher, capacity | ← students, teachers, courses |
| `guardians` | `text id` | name, **phone (حسّاس)** | ← students, profiles |
| `teachers` | `uuid` | name, specialty, circle_id | → circles |
| `students` | `bigserial` | full_name, **national_id/mobile (حسّاس PDPL)**, points, streak, status | → guardians, circles |
| `courses` | `bigserial` | type(enum), daily_target | → students |
| `sessions` | `bigserial` | start/stop_ayah, performance, attendance, itqan | → students, courses, teachers |
| `mistakes` | `bigserial` | type(enum), required_reps (100/45/20) | → sessions |
| `points` | `bigserial` | value, reason, source_event | → students |
| `payments` | `bigserial` | payer, amount, kind(enum), status | — |
| `waitlist` | `bigserial` | full_name, phone, status(enum) | — |
| `harvest` | `bigserial` | scope, parts, status(enum) | → students |
| `rewards` | `text id` | name, cost, stock, +category/description/hot (من `market.sql`) | ← orders |
| `orders` | `bigserial` | items(jsonb), total, status(enum) | → students |
| `audit_log` | `bigserial` | actor, action | — |
| `profiles` | `uuid=auth.users` | phone, full_name, role, status, student_id, guardian_id | → students, guardians |
| `teacher_applications` | `bigint identity` | **بيانات هوية كاملة (حسّاس)**, status, source | — |

الأنواع (enums) معرّفة بعناية (student_status، mistake_type، payment_kind…). النمذجة **جيدة ومطابقة لوثيقة SRS**.

### مخرج `npx prisma migrate status`

**غير قابل للتطبيق** — لا Prisma. الأخطر: **لا يوجد أي نظام ترحيلات مؤتمت إطلاقًا**. الترحيلات ٨ ملفات SQL منفصلة يجب تشغيلها **يدويًا وبالترتيب الصحيح** في لوحة Supabase. لا مصدر حقيقة واحد، ولا سجل تطبيق، ولا تراجع (rollback). الترتيب موثّق في التعليقات فقط:
`schema.sql` → `auth_profiles.sql` → `rls_policies.sql` → `market.sql` → `sql/01` → `sql/02` → `sql/03` → `sql/04`.
وهناك **تكرار وتداخل** بينها (روابط الملف الشخصي معرّفة في `auth_profiles.sql` **و** `sql/01`؛ ودوال `app_is_staff` معرّفة في `rls_policies.sql` ثم **يُعاد تعريفها** في `sql/03`). هذا دَيْن ترحيلات حقيقي.

### هل الاتصال بـ Supabase يعمل؟

**تعذّر التحقق حيًّا.** بيئة التدقيق تحجب كل الاتصالات الصادرة بسياسة شبكة (الوكيل ردّ `403 CONNECT` حتى على `example.com`). لذا لم أستطع اختبار REST أو RLS فعليًّا. ما يمكن تأكيده من الكود:
- الإعداد موجود وصحيح الشكل في `js/config.js` (رابط مشروع + مفتاح عام).
- منطق `DB.init()` سليم، ويسقط تلقائيًّا لوضع Offline إن غاب الاتصال.

**يجب اختباره يدويًّا (لم يُنفَّذ هنا):**
```
# الاختبار الحاسم لـ RLS — يجب أن يعيد [] أو 401/403، لا بيانات:
curl -H "apikey: <anon>" "<url>/rest/v1/students?select=id&limit=1"
```
إن أعاد صفوف طلاب لمفتاح anon → **RLS غير مطبّقة والبيانات الحسّاسة للقُصّر مكشوفة** (انظر القسم ٤).

### الفجوة: نماذج تستدعيها الواجهة ولا وجود لها

- **الجداول:** ✅ لا فجوة — كل الجداول الـ١٤ التي تستدعيها `db.js` عبر `.from()` موجودة في المخطط.
- **دوال RPC:** 🔴 **فجوة مؤكّدة** — الواجهة تستدعي `admin_link_user` (في `db.js:217`، ميزة «ربط الحساب بالطالب/العائلة» بشاشة إدارة المستخدمين)، لكن هذه الدالة **غير معرّفة في أي ملف SQL** (المعرّف فقط `admin_set_role` و`admin_set_status` في `sql/02`). النتيجة: ميزة الربط **ستفشل وقت التشغيل** بخطأ «الدالة غير موجودة» ما لم تُنشأ يدويًا في القاعدة دون إيداعها في المستودع.

---

## ٣. البيانات الوهمية والديون

### بيانات وهمية / بذور مضمّنة (Hardcoded)

المشروع مبنيّ أصلًا كـ«نموذج أولي عامل ببيانات في الذاكرة» (تصريح صريح في `app.js:3`). أبرز الكتل:

| المسار:السطر | المحتوى |
|---|---|
| `js/app.js:21-24` | `CIRCLES` — حلقتان مضمّنتان |
| `js/app.js:30-41` | `STUDENTS` — ١٠ طلاب وهميين بالكامل |
| `js/app.js:42-46` | `GUARDIANS` — ٣ عائلات |
| `js/app.js:47-51` | `WAITLIST` — ٣ طلبات انتظار |
| `js/app.js:52-56` | `PAYMENTS` — ٣ مدفوعات |
| `js/app.js:58-71` | `REWARDS` — ١٢ جائزة |
| `js/app.js:75-80` | `NOTIFS` — إشعارات ثابتة |
| `js/app.js:81-89` | `AUDIT` + `HARVEST` ثابتة |
| `js/app.js:479` | `EXCUSES` — عذر واحد ثابت |
| `js/app.js:1167` | `APPROVALS` — موافقتان ثابتتان |
| `js/app.js:1222` | «رمز QR» — مصفوفة بتّات ثابتة (وهمي) |
| `js/app.js:340-343` | صفوف ورقة المتابعة مولّدة حسابيًّا (`i%4===0?...`) |
| `js/app.js:1243-1246,1250-1252` | أرقام تقارير الأثر ثابتة (٨٣٪/٧٨٪/٩١٪) |
| `js/app.js:403-404` | مؤشرات أداء ثابتة (P95=١٢ث، تحميل ١٫٤ث) |

> ملاحظة مهمة: البذور المحلية **قصدية** كخطة سقوط (Offline-first) — عند نجاح الاتصال تُستبدل بـ`applyRemote()` (`app.js:1264`). لكن الشاشات المصنّفة 🔴/🟠 أعلاه لا تملك مسار حفظ حقيقيًّا، فتبقى بياناتها وهمية دائمًا.

### `TODO` / `FIXME` / `console.log`

- **`TODO`/`FIXME`/`XXX`/`HACK`: لا يوجد ولا واحد** حقيقي. (نتائج البحث عن `xxx` كانت أرقام جوال نائبة مثل `0555xxx123`، ليست ديونًا.)
- **`console.log`: لا يوجد.**
- **`console.warn`/`console.error`: ٩ مواضع، كلها في `js/db.js`** — معالجة أخطاء مقصودة وسليمة (سقوط Offline، فشل مزامنة). نظيف.

---

## ٤. المصادقة والصلاحيات

### آلية المصادقة

- **Supabase Auth** (بريد + كلمة سر). حيلة ذكية: رقم الجوال يُحوَّل لبريد اصطناعي داخلي `u<أرقام>@albrrak.app` (`db.js:29-34`) لتفادي رسائل SMS، ويُقبل البريد الحقيقي كما هو.
- ⚠️ **قيد موثّق:** نطاق `albrrak.app` الاصطناعي يتطلّب سجلّ MX ليقبله Supabase؛ لذا حساب المشرف يجب أن يستخدم بريدًا حقيقيًا (تعليق `db.js:27-28`).
- التسجيل الذاتي محصور بدورين فقط (`parent`, `student`) — دفاع في العمق (`db.js:36,39`).

### أين تُعرَّف الأدوار

- **٧ أدوار:** general/teacher/admin/examiner/parent/student/sponsor.
- في الواجهة: `ROLES` (`app.js:92-121`) للقوائم + `ROLE_LABELS` (`app.js:1290`).
- في القاعدة: عمود `profiles.role` + دوال `app_role()`/`app_is_staff()`/`app_is_admin()`/`app_is_general()`.
- الدور الفعلي يُطبَّق بعد الدخول من `profiles.role` (`applyProfileRole`, `app.js:1355`).

### هل يوجد middleware حماية؟ ما المحميّ فعلًا؟

**لا يوجد middleware** (لا خادم). الحماية طبقتان:
1. **واجهة (تجميلية فقط):** إظهار/إخفاء عناصر حسب الدور — **قابلة للتجاوز بسهولة** من أدوات المطوّر. ليست حماية.
2. **الحماية الحقيقية = RLS + دوال RPC بـ`security definer`** على مستوى القاعدة. هنا الأمان الفعلي:
   - العمليات الحسّاسة (تغيير الأدوار/الحالة) عبر RPC تتحقّق `app_is_general()` داخليًّا وترفض غير المشرف (`sql/02`).
   - منع تصعيد الصلاحيات: التسجيل الذاتي مقيّد بـ`parent/student` + Trigger `guard_profile_role` يمنع تغيير الدور ذاتيًّا (`rls_policies.sql`).
   - منع المشرف من تعطيل نفسه (`sql/02`).

### هل RLS مفعّل؟ على أي جداول؟

**نعم في ملفات SQL** — RLS مُفعّل على كل الجداول (١٦ جدولًا) وبسياسات إنتاجية بالأدوار في `rls_policies.sql` + `market.sql` + `sql/01/03/04`:
- الكادر: وصول كامل للجداول التشغيلية.
- ولي الأمر: يقرأ أبناءه فقط. الطالب: يقرأ سجلّه فقط.
- `teacher_applications`: إدراج عام (رابط التقديم) + قراءة للكادر فقط (بيانات هوية حسّاسة).
- `orders`: الطالب يقرأ/ينشئ طلباته، الكادر يدير الكل.
- الحساب المعطَّل يفقد كل الوصول (`sql/03`).

### 🔴 المخاطرة الحرجة في RLS

المخطط الأساسي `schema.sql:180-200` ينشئ سياسة مفتوحة تمامًا `p_all ... using(true) with check(true)` على كل الجداول. ملف `rls_policies.sql` **يُبطلها** ويستبدلها. **لكن هذا يعتمد كليًّا على تشغيل الملفات اللاحقة يدويًّا وبالترتيب الصحيح.** إن شُغّل `schema.sql` فقط ونُسي `rls_policies.sql`:
> **كل بيانات القُصّر الحسّاسة (`national_id`, `mobile`, `phone`) تصبح مقروءة ومكتوبة لأي شخص يملك المفتاح العام** — والمفتاح العام **مرفوع في المستودع** (`js/config.js`). هذه ثغرة PDPL خطيرة إن لم تُطبَّق السياسات فعليًّا. **يجب التحقق يدويًّا** (اختبار القسم ٢).

---

## ٥. الجودة والمخاطر

### `npx tsc --noEmit`
**غير قابل للتطبيق** — لا TypeScript ولا `tsconfig.json`. الكود JS خام.

### `npm run lint`
**غير قابل للتطبيق** — لا `package.json` ولا ESLint. لا يوجد أي linter مُعدّ.

### `npm run build`
**غير قابل للتطبيق** — لا خطوة بناء. النشر = رفع الملفات الثابتة كما هي إلى Vercel.

### `npm audit --production`
**غير قابل للتطبيق** — لا `node_modules` ولا `package-lock`. الاعتماد الوحيد `@supabase/supabase-js@2.45.4` من CDN، **مثبّت على إصدار دقيق** (جيد أمنيًّا: لا انجراف إصدار).

### الفحص البديل الذي أُجري فعلًا (متاح في هذه البيئة)

| الفحص | النتيجة |
|---|---|
| `node --check js/app.js` | ✅ صياغة سليمة |
| `node --check js/db.js` | ✅ صياغة سليمة |
| `node --check sw.js` | ✅ صياغة سليمة |
| `JSON.parse vercel.json` | ✅ صالح |
| علامات تعارض git متبقية | ✅ لا شيء |
| `console.log` متسرّبة | ✅ لا شيء |

### مشاكل RTL / الخطوط العربية / التوطين

- **RTL:** ✅ ممتاز — `<html dir="rtl" lang="ar">`، والتصميم كله مبنيّ على RTL أصالةً، والأرقام تُعرَّب عبر `arNum()` (`toLocaleString('ar-EG')`).
- **الخطوط:** IBM Plex Sans Arabic + Amiri من Google Fonts. ⚠️ اعتماد على شبكة خارجية — إن حُجبت Google Fonts يسقط لخط النظام (CSP يسمح بها).
- **التوطين:** أحادي اللغة (عربي فقط) — لا نظام i18n، النصوص مضمّنة في الكود. مقبول لجمهور مستهدف عربي، لكن غير قابل للتوسّع للغات أخرى دون إعادة عمل.
- **ملاحظة CSP إيجابية:** `vercel.json` يضبط رؤوسًا أمنية قوية (CSP، HSTS، X-Frame-Options: DENY، nosniff، Permissions-Policy). جيد جدًّا.

---

## ٦. Git

```
الفرع الحالي: claude/market-ecommerce
الحالة: working tree clean — لا تغييرات غير مُودعة
التتبّع: up to date with origin/claude/market-ecommerce (لا commits غير مدفوعة)
```

**آخر ٢٠ commit** (مختصر): يبدأ بدمج أحدث `main` في فرع المتجر (`0b9d0d2`)، ثم تطوير المتجر الإلكتروني، ثم إدارة الطلاب/الحلقات/المستخدمين، ثم نظام طلبات المعلمين، ثم سلسلة إصلاحات (الشاشة البيضاء، Service Worker، رابط Supabase SDK، الدخول بالبريد/الجوال).

**الفروع:**
- محليًّا: `claude/market-ecommerce` (الحالي)، `claude/new-session-n3qt1o`، `main`.
- على الريموت: `origin/main`، `origin/claude/market-ecommerce`، `origin/claude/new-session-n3qt1o`، `origin/claude/repo-diagnostic-review-s81f0f`.

**تغييرات غير مرفوعة؟** لا. **الفرع متأخر عن الريموت؟** لا — متطابق تمامًا. (هذا الملف `DIAGNOSIS.md` أُنشئ ولم يُودَع بعد — قرار الإيداع لك.)

---

## ٧. الحكم النهائي

### جدول الجاهزية بالوحدات

| الوحدة | الحالة | نسبة الجاهزية |
|---|---|---|
| البنية التحتية (نشر ثابت + Supabase + PWA + CSP) | حقيقي | ٨٥٪ |
| المصادقة (دخول/تسجيل بجوال أو بريد) | حقيقي | ٨٠٪ |
| RLS والصلاحيات (في الملفات) | حقيقي — **غير مُتحقَّق حيًّا** | ٧٠٪ |
| التسميع اليومي + النقاط | حقيقي | ٨٥٪ |
| سوق الحلقة (المتجر بالنقاط) | حقيقي | ٨٥٪ |
| القبول وقائمة الانتظار | حقيقي | ٨٠٪ |
| إدارة المستخدمين | حقيقي — **معطوب جزئيًّا** (`admin_link_user` مفقود) | ٦٠٪ |
| إدارة الطلاب/الحلقات/العائلات | حقيقي | ٧٥٪ |
| طلبات المعلمين | حقيقي | ٨٠٪ |
| لوحة القيادة/البورصة/الشاشة | محسوب من الذاكرة | ٧٠٪ |
| التقارير | واجهة + مؤشرات مختلطة | ٤٠٪ |
| البوابة المالية | واجهة + سجل بلا بوابة دفع | ٣٥٪ |
| الحصاد | ذاكرة فقط (بلا حفظ) | ٣٠٪ |
| ورقة المتابعة الشهرية | بيانات مُلفّقة | ٢٠٪ |
| الإشعارات | ذاكرة + جدول ثابت | ٢٠٪ |
| الأعذار / الربط / الموافقات | واجهة/ذاكرة فقط | ١٥٪ |
| سجل QR | وهمي بالكامل | ٥٪ |
| تقارير الأثر (الداعم) | أرقام ثابتة | ١٠٪ |
| **الإجمالي المرجّح للمنتج** | **نموذج متقدّم — نصفه حقيقي** | **≈ ٥٥٪** |

### الحاجبات (Blockers) — مرتّبة بالأولوية

1. **🔴 التحقق من إنفاذ RLS فعليًّا على القاعدة الحيّة** — أخطر بند. المفتاح العام مرفوع في git؛ إن لم تُطبَّق `rls_policies.sql` (وبقيت `p_all using(true)`)، فبيانات القُصّر الحسّاسة (هوية/جوال) مكشوفة للعالم. **يجب اختبارها يدويًّا فورًا** قبل أي مستخدم حقيقي.
2. **🔴 دالة `admin_link_user` مفقودة** — ميزة ربط الحسابات بالطلاب/العائلات تفشل وقت التشغيل. إمّا إضافة تعريفها في ملف SQL مُودَع، أو حذف استدعائها.
3. **🟠 غياب نظام ترحيلات مؤتمت** — ٨ ملفات SQL يدوية متداخلة/مكرّرة بلا مصدر حقيقة ولا سجل تطبيق. أول خطأ ترتيب = قاعدة نصف-مهيّأة. خطر تشغيلي متكرر.
4. **🟠 بوابة الدفع غير موجودة** — «البوابة المالية» تسجّل صفوفًا فقط؛ لا Apple Pay/STC/مدى فعليًّا. أي حديث عن «تحصيل مالي» اليوم غير صحيح.
5. **🟡 شاشات وهمية تُقدَّم كأنها تعمل** — ورقة المتابعة، الإشعارات، الحصاد، الأعذار، الربط، الموافقات، QR، الأثر. خطر توقّعات: تبدو مكتملة بصريًّا لكنها بلا حفظ/منطق حقيقي.
6. **🟡 صنف الاعتماد على CDN خارجي** — Supabase SDK والخطوط من CDN؛ حجبها يكسر وظائف. (مخفّف بـOffline-first للبيانات، لا للـSDK.)
7. **🟡 غياب كامل لأدوات الجودة** — لا linter، لا فحص أنواع، لا اختبارات، لا CI للكود (Vercel ينشر فقط). ملف ١٤٠٨ أسطر واحد بلا شبكة أمان.

### التوصية الصريحة

**ابْنِ على الموجود — لا تُعِد الهيكلة.** والسبب بصراحة:

- **النواة سليمة وحقيقية.** التسميع، المتجر، القبول، المصادقة، إدارة المستخدمين/الطلاب، وRLS بالأدوار — كلها مكتوبة بعناية ومطابقة لوثيقة SRS، وتعمل فعليًّا مع Supabase. إعادة الهيكلة تهدر عملًا جيدًا.
- **لكن لا تُصدّق «٥٥٪» على أنها منتج.** ما لديك **نموذج أولي متقدّم (Advanced Prototype)** نصفه مربوط بقاعدة حقيقية ونصفه واجهات عرض. الفجوة بين «يبدو مكتملًا» و«يعمل فعلًا» كبيرة وخطيرة على التوقّعات.

**الترتيب العملي قبل أي تطوير جديد:**
1. تحقّق من RLS حيًّا (بلوك #1) — **قبل أي شيء آخر**. أمان القُصّر مسؤولية قانونية.
2. أصلح `admin_link_user` (بلوك #2) — إصلاح صغير يُعيد ميزة معطوبة.
3. وحّد الترحيلات في مصدر واحد مُرتَّب (ولو مجلّد `migrations/` مرقّم بـREADME صارم).
4. اتّخذ قرارًا صريحًا لكل شاشة 🔴: إمّا ربطها بالقاعدة، أو إخفاؤها حتى تُبنى — لا تتركها تُوهم الاكتمال.
5. البوابة المالية: عطّلها أو صرّح بأنها «تجريبية» حتى يُدمَج مزوّد حقيقي.

**متى تُعاد الهيكلة جزئيًّا؟** فقط عند تجاوز حجم `app.js` (١٤٠٨ أسطر بملف واحد) حدَّ الصيانة — عندها جزّئه لوحدات (modules) دون تغيير المكدّس. لا حاجة لـReact/Next ما لم تتطلّب متطلبات جديدة توجيهًا حقيقيًّا أو SSR/SEO (حاليًّا `noindex` متعمّد، فلا داعي).

---

*انتهى التقرير. لم يُعدَّل أي ملف مصدر؛ الاتصال الحيّ بـSupabase لم يُختبر بسبب حجب الشبكة في بيئة التدقيق — بنود التحقق اليدوي موسومة أعلاه بوضوح.*
